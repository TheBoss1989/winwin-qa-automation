{
  "name": "winwin-qa-automation",
  "version": "1.0.0",
  "private": true,
  "description": "Playwright automation test task for WinWin.travel",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.56.0"
  }
}
